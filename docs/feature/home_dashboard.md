# ホームダッシュボード

## 仕様

- トップナビゲーションとボトムナビゲーションの間に、ホームのダッシュボードを表示する。
- カレンダーアイコンの右に当日の `日 / 月` を表示する。日付はホームデータの `activityDate` から生成する。
- 日付の下に学習状況を示す丸を並べ、丸の色は緑（完了）、紫（当日・進行中）、灰色（予定）の3色に限定する。
- 火のアイコンと連続学習日数を表示する。
- 再生ボタンの下に、ホームデータのプログラム一覧と追加枠を表示する。
- プログラム一覧の下に `REVIEW PROGRAM` と「復習する」ボタンを表示する。
- トップナビゲーションおよびボトムナビゲーションのレイアウト・描画は変更しない。
- 画面レイヤーはホーム本体、トップナビゲーション、ボトムナビゲーションの順に重ね、ナビゲーションを前面に表示する。
- ホーム本体の上端はトップナビゲーションの高さとステータスバー領域を考慮して配置する。
- 画面全体の背景色は白とする。
- ホーム本体には、白背景を保ったまま、必要な箇所だけに細いセクション見出し、学習状況をつなぐ細線、再生ボタン周囲の短い紫系の軌道線、プログラム名の補助ラベルを追加する。
- 装飾は学習情報の視認性を妨げない範囲に限定し、グラデーションとカード型 UI は使用しない。
- 日と月の表示には同梱した `Jua` フォントを使用し、日を大きく、月をやや小さく表示する。
- 上部の日付・連続学習日数は、参考デザインに合わせた大きさと間隔を保ち、左右にゆとりを持たせて配置する。狭い画面では日付部分のみを必要な範囲で縮小する。
- 連続学習日数は炎アイコンと文字の下辺を揃え、両者の間隔を `8`、右側の余白を `9` とする。数字は `Jua` の `34`、単位は標準フォントの `24` で表示する。
- 学習状況の丸は通常 `14`、選択中は `22` の大きさとし、各要素の間隔を `11` とする。
- 再生ボタンは直径 `254`、枠線幅 `9` で表示する。プログラムアイコン列との間には `24` の余白を設ける。
- プログラムアイコンは `36` の大きさとし、各アイコンの間隔を `23` とする。

## データ構成

- 表示データは `HomeDashboard` に集約する。
- `HomeDashboardRepository` をデータ取得の境界とし、現在は `MockHomeDashboardRepository` を使用する。
- 将来の API 接続では `GET /v1/home` の `activity_date`、`tasks`、`progress.streak_days` から表示モデルを組み立てる。

## 構成

- 画面: `lib/features/home/presentation/home_tab_page.dart`
- 表示モデル: `lib/features/home/domain/home_dashboard.dart`
- Repository: `lib/features/home/domain/home_dashboard_repository.dart`
- モック実装: `lib/features/home/data/mock_home_dashboard_repository.dart`
- フォント: `assets/fonts/Jua-Regular.ttf`

## 実装状況

実装済み。現在日付を使ったモック表示、ステータス色、連続日数、控えめなセクション装飾、プログラムアイコン列、復習ボタンを表示している。
