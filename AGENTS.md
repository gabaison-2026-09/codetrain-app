# AGENTS.md

## 基本方針

* 本プロジェクトは Flutter / Dart の標準的な設計・命名規則を基本とし、本ファイルおよび開発規約に従って実装する。

* 一度採用されたプロジェクト内の構成・設計・命名・依存関係との一貫性を維持し、変更は必要最小限にする。

* 新しい設計パターン、ディレクトリ構成、命名方式を不用意に追加せず、同じ目的で複数の方式を混在させない。

* 既存コードの大規模なリファクタリング、ファイル移動、命名変更は、依頼された機能の実装に必要な範囲を超えて行わない。

* 状態管理、ルーティング、DI、API 通信、エラーハンドリング等は、プロジェクトですでに採用されている方式を優先する。同一目的のために別方式を新たに持ち込まない。

* 新しい依存パッケージを追加する前に、Flutter / Dart 標準機能または既存依存で実現できないか確認する。既存の手段で実現可能な場合は、新規依存を追加しない。

## 規約の適用範囲と優先順位

規約が競合した場合は、次の優先順位で判断する。

1. `AGENTS.md`
2. [`docs/development/*.md`](docs/development/)
3. [`docs/feature/*.md`](docs/feature/)
4. 既存実装

既存実装と規約に差異がある場合は、無関係な既存コードを一括修正せず、今回の変更範囲でどちらに合わせるべきか判断する。判断できない場合は、実装前にユーザーへ確認する。

変更内容に関連する [`docs/development/`](docs/development/) の規約は、実装前に確認する。アーキテクチャ、命名、テストなど複数の規約に関係する場合は、該当するすべての規約を確認する。

## `docs/feature/` の運用

実装する機能の仕様、前提、実装状況、変更点は [`docs/feature/`](docs/feature/) 配下の Markdown ファイルに記録・更新する。対応するドキュメントがなければ、機能ごとに `.md` ファイルを作成する。

機能仕様書を追加・削除・変更した場合は、一覧ファイル [`docs/feature/README.md`](docs/feature/README.md) も更新し、機能名、用途、主な実装箇所が確認できる状態を維持する。

Must Ask の確認待ちでコード・設定・依存関係を変更できない場合でも、仕様の整理、影響範囲の調査、`docs/feature/` への案の記録までは行ってよい。

## Must Ask（事前確認必須）

以下に関わる仕様・実装・設定変更は、ユーザーの明示的な確認を得るまで着手してはいけない。

対象機能について、実際の挙動、データ処理、権限、外部サービス、依存関係、プラットフォーム設定に直接的または間接的な変更が生じる場合を対象とする。

単純な文言修正や、既存仕様の範囲内での表示調整のみで、挙動・データ処理・権限・外部サービス・依存関係・プラットフォーム設定に影響しないことが明確な場合は対象外とする。

* 広告、広告識別子、トラッキング、同意取得

* 課金、サブスクリプション、決済、購入復元、paywall

* プライバシー、個人情報、権限、データ収集・共有・保持

* プッシュ通知、ローカル通知、通知権限、通知トークン

* ユーザーデータの削除、退会、アカウント削除、保持期間

対象に該当する、または該当する可能性を合理的に否定できない場合は、実装前に以下を明示して確認を求める。

1. 変更対象の機能と影響範囲
2. 保存・送信・共有されるデータ、または外部サービスとの連携
3. ユーザー体験、権限、費用、ストア審査・法令対応への影響
4. 実装してよいかどうか

確認が得られるまでは、コード・設定・依存関係を変更しない。明示的な確認は、別の機能に対する確認や一般的な実装依頼から推測してはいけない。

## 作業フロー

1. 関連する [`docs/feature/*.md`](docs/feature/)、既存実装、既存テストを確認する。
2. 変更内容に関連する [`docs/development/`](docs/development/) の規約を実装前に確認する。
3. 既存の設計、状態管理、ルーティング、依存関係、命名・配置規則を確認する。
4. Must Ask の対象か判定する。対象の場合は実装を停止し、必要事項を整理して確認を求める。
5. 対応する機能ドキュメントがなければ作成し、仕様、前提、実装方針を記録する。
6. 既存構成に沿った必要最小限の変更で実装する。
7. 可能な範囲で関連するテストを追加・更新し、[`docs/development/testing.md`](docs/development/testing.md) に従って検証する。
8. 実装結果に合わせて `docs/feature/` のドキュメントと [`docs/feature/README.md`](docs/feature/README.md) を更新し、仕様と実装状況を一致させる。
9. 変更内容、検証結果、実行できなかった確認、残っている問題や未確認事項を簡潔に報告する。
10. 最終出力の最後に、今回の変更内容に対応する簡潔なコミットメッセージを日本語で提案する。コミットメッセージはメッセージ部分のみをコードブロックで記載し、コードブロックの後に文章を続けない。

```text
<コミットメッセージ>
```
